# quick edit zsh aliases
alias zsh_aliases="vim ~/zsh_aliases/.zsh_aliases"

# quick edit zshrc
alias zshrc="vim ~/.zshrc"

# system command overrides
alias diff="colordiff"
alias df="df -H"
alias du="du -ch"
DEPLOY_CONFIG_PATH='~/deploy-config/config'
if [ -f ${DEPLOY_CONFIG_PATH} ]; then
	alias ssh="ssh -F '$DEPLOY_CONFIG_PATH'"
fi

# apple aliases
alias show_hidden="defaults write com.apple.finder AppleShowAllFiles YES && killall Finder"
alias hide_hidden="defaults write com.apple.finder AppleShowAllFiles NO && killall Finder"

# useful tools
alias ip="wget http://nwdesign.us/myip.php -O - -q ; echo"

# update everything
alias update="softwareupdate -i -a; brew update; brew upgrade; brew cleanup; brew cask cleanup; brew cask outdated | xargs brew cask reinstall; brew prune; brew doctor;"

# Laravel
alias artisan="php artisan"

# git
alias pull="git pull"
alias push="git push"
alias force="git push --force-with-lease"
alias go="git checkout"
alias fetch="git fetch --all"
alias commit="git add .; git commit -am"
alias merge="git merge"
alias status="git status -sb"
alias composer="./composer.phar"
alias dep="./deployer.phar"
alias bower="node_modules/bower/bin/bower"
alias rebase="git fetch --all; git rebase -i"
alias reset="git reset --hard"

# navigation
alias home="cd ~"
alias ..="cd .."
alias ...="cd ../.."

# Homestead
alias homestead='function __homestead() { (cd ~/Homestead && vagrant $*); unset -f __homestead; }; __homestead '
